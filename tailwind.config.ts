/**
 * Tailwind v4 utilise une configuration CSS-first via `@theme` dans `src/styles.css`.
 * Toutes les couleurs nommées (bg-surface, text-primary, border-outline-variant, etc.)
 * sont définies là-bas. Ce fichier reste vide pour compatibilité d'outils
 * et documentation.
 */
export default {};
